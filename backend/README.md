# Backend

Minimal backend scaffold for future API development.

## Current Status
- No backend implementation is required yet.
- `src/` is reserved for future services and routes.
- `tests/` is reserved for future backend test coverage.

## Setup Later
- Create a virtual environment
- Install with `pip install -r requirements.txt`
